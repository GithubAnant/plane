import { Vector3 } from "three"

export const EVENTS = {
    PARTICLES: {
        BUBBLE: {
            EMIT: "PARTICLES_BUBBLE_EMIT",
        },
        WAVE: {
            EMIT: "PARTICLES_WAVE_EMIT",
        },
    }
}